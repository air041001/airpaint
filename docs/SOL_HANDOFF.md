# P2 后的有限维护与实测计划

更新：2026-09-13。P2 工程基线为 `c175e11`；候选加载语法修复为 `1310c20`，本次继续完成“补齐特殊 LoRA 模板执行”。本文是用户明确要求的一份交接计划，不自动授权开启新产品阶段。

## 当前优先事项：模板与构图检查冲突（已修复，待用户实测）

本节优先于下文旧的“已完成/重启复测”描述：模板执行代码已落地，但真实使用暴露翻译失败，尚未通过稳定性验收。用户现在要求 sol 先修这次回归，再完成有限实测；不启动通用 Agent 或工作流扩展。

### 事实、判断与证据边界

- 用户转交的持久化会话报告：连续 7 次 `/api/translate` 返回 502，同期其他接口正常；同一请求的两次模型回复结构完整但被多人构图检查拒绝，约 44 秒失败；去掉模板后一次成功约 32.8 秒。本轮未重读那份日志、未重放这些付费调用，数字作为外部会话报告保留，不冒充独立测量。sol 先定位已有日志证据；缺失就写明，不为了补数字重复请求模型。
- 本轮直接核对代码：`server/prompt_engine.py::_composer_multi_prompt_shape_issue` 全局扫描多人 Prompt/IR，下半身可见要求与 close-up/upper body 同时出现即拒绝；函数没有接收可信的模板布局状态。`siliconflow_translate` 最多两次尝试，语义校验失败同样进入修复重试，最终可经 translate 转为 502。模板上下文却允许同画布多分镜，旧 system 中也存在单镜头裁切/短句限制，两者有冲突。
- 判断：这是 P2 模板执行与原单镜头校验的兼容回归，不是等待未来阶段才应解决的能力；模型是否将分镜词写进正文会影响触发，因此仅看六行协议完整不能判定成功。不要把原因简化为 USAGE 缺行、Qwen、ComfyUI 或网络。
- HTTP 502 与浏览器没有取得响应的 `Failed to fetch` 分开处理。后者可能是 Tunnel/TUN、连接中断或其他浏览器传输问题；具体原因需浏览器 Network 与后端请求对应，不能由提示文本直接断定。二者可能同时存在。

### 完成结论（2026-09-14）

- R1 已完成：有效布局只由服务端已校验的所选模板与明确单格覆盖派生，并作为显式参数贯穿 Composer system/repair、协议解析、可画性检查、多人构图检查和最终 tag 护栏。可信多格允许不同分镜混合全身、可见脚部、上半身与近景；抽象防失败措辞、人数/身份、LoRA 选择和协议检查仍保留。
- 无模板或仅用户正文写 `multi-panel` 不能获得布局权限；明确单格继续触发原裁切冲突；“画一张”不会被识别为单格。参考图重编译链已用 mock Vision + 真实 Composer CLIENT 响应解析覆盖，合法多格一次调用通过并保留模板正向与最终负面。
- R2 已完成：Composer 两次校验失败返回 `composer_validation_failed`，上游 HTTP 失败与响应异常使用不同类别；浏览器分别显示服务端 HTTP 错误、非 JSON 响应、fetch 传输失败和取消/超时，不进行自动重试且不清空当前输入/模板选择。
- 当前 `server/state/logs` 未找到可用于独立复核交接所述七次失败的对应语义错误文本；本轮没有为补数字调用真实模型或 GPU。R3 仍需用户重启/刷新后做一次有限预览和自主生图判断。

### 修复批次 R1：使布局上下文进入校验（已完成）

目标：多格画布不同分镜允许分别全身、半身、脸部；明确单格仍适用原裁切校验。允许修改 prompt_engine、必要的调用参数与测试；不改真实 Registry/数据库，不重写模板或增添特定 FComic 名称分支。

1. 从服务端已校验的模板解析结果派生有效布局模式，结合现有 explicit_single_panel 得到 multi_panel/single_panel。以显式参数传入 Composer 和相应校验，不靠扫描原始 description、Prompt 中出现某个英文词或客户端自报标记提权。保持无模板调用方的旧默认。
2. 只对“跨分镜被误当同一镜头”的规则调整适用范围：裁切互斥、单连续构图及相关 prose 限制逐条确认。多格时不要对整段跨镜头文本做互斥判断；若没有可靠的逐格结构，不发明新的逐格 IR，也不伪装成已完成逐格验证。语法、主体身份/计数、LoRA ID/权重、用户锁和禁止伪造资源等检查继续保留。
3. 同步 system 与 repair 提示的布局语义，避免代码放行但模型仍被指令要求删掉分镜。保持作者骨架与 Composer 补充的职责；用户明确单格时沿用已实现的布局停用规则。参考图和暗房重编译也需传递同一有效布局，不能只修普通 translate 入口。
4. 不以提高重试次数、扩大超时、忽略全部 RuntimeError、关闭多人校验或让模型删模板来解决。合法多格回复应第一次通过；真正不合法输出仍保持有限修复，调用次数可核对。

最低验收用安全虚构漫画文本，mock 外部模型但执行真实 CLIENT 返回解析、translate、预览/最终化路径：

- 已选有效多格模板，正文同时有 full body/visible feet 与 upper body/close-up：一次 Composer 调用通过，模板骨架和最终负面保留。
- 同一资产无模板、明确单格覆盖模板：相同的真实单镜头冲突仍被原规则识别；“画一张”不视为单格。
- 只有原始描述或用户正文提到 multi-panel，但没有可信有效布局：不能借此绕过基于所选模板开放的校验路径。
- 角色计数/身份、非法模板、坏协议仍拒绝；普通 LoRA、manual final、暗房无 delta 零翻译行为不回归。
- 参考/暗房重编译至少一个实际调用链覆盖布局参数传递；不要仅 mock 掉受检函数或复制规则写测试。

### 修复批次 R2：区分错误与控制重试成本（已完成）

R1 通过后检查 `web/index.html::api` 和后端翻译错误返回。保留 HTTP 状态与可理解的失败原因，区分服务已返回 502、请求在浏览器失败、主动取消/超时。可用小型 error_kind 或请求关联标识帮助对照日志，优先复用现有字段，不建设大型日志系统。

模板/构图拒绝应显示“构思校验未通过”等可操作信息，不能统一写成网络断开；纯传输失败不得声称模型未运行或任务未创建。保存当前草稿/模板选择，不能自动重新生图或形成重复提交。错误日志不含密钥、完整作者原文或默认记录完整用户隐私内容；内部使用校验类别、有效布局、尝试次数和耗时即可。

验收 mock HTTP 502 JSON、非 JSON 错误响应、fetch 拒绝三种情况，页面不崩溃、草稿仍在、无无限重试。Tunnel/TUN 如果仍有独立故障，记录可复现时间与请求路径后另行定位；不能用 R1 成功证明网络问题已解决。

### 验收批次 R3：用户实测后暂停

先完成隔离回归再提交/push。使用当前失败请求作参照；若需要真实模型复测，先利用已有日志，确需调用则事先明确次数与目的，优先让用户自行预览。结构修复不需要先调用 GPU。预览通过后由用户决定是否生图，并分别判断模板是否执行、图片质量是否合格。

保留普通 LoRA 对照与明确单格对照，不恢复无目标大批量抽图。第一次真实预览成功不证明统计稳定；报告实际次数，不声称“永不失败”。同一问题两次定向修复仍失败时，带最小复现、模型响应校验结果和已尝试方案请原主 Agent 协助。验收结束继续暂停。

### P3/P4 编号澄清

当前有效 LoRA 契约只有 P1、P2A、P2B 与 §10.1 模板执行补充，没有可以据此断言“尚欠实现”的本轮 P3/P4 定义。旧搬运任务书已清理，未恢复的旧编号含义不得猜测。历史 D38/DEVLOG 的 Phase 3 Character Knowledge 已验收，Phase 4 PromptState 是延后的另一条路线，不能当成本轮剩余清单。

因此本交接用 R1（兼容回归）、R2（错误区分）、R3（用户实测）明确当前待办。用户若提供旧 P3/P4 原文，再核对哪些已被后续模板执行覆盖；不要为了凑阶段编号启动新的模板系统、PromptState 或 Agent 重构。

## 当前结论与边界

P2 工程已交付；用户浏览器测试暴露了特殊模板未执行。先读 `E:/comfy-web/AGENTS.md`、`E:/comfy-web/docs/BUILDHANDOFF.md`，具体协议查 `E:/comfy-web/docs/LORA_USAGE_CONTRACT.md` §8–10。不要通读已废弃的搬运任务书，勿按任务或测试轮次机械追加 DEVLOG/ADR。

2026-09-13 对 `fcomic1to1000_anima_v1` 的真实失败与修复：

- Registry 文件为 `E:/comfy-web/server/lora_registry.yaml`，资产类型 style、trigger_policy=none、required_tags=[]、model/clip 强度均为 1.0；本地模型文件存在。先前误把加载标签当 trigger 的候选已被用户修订，没有进入当前条目。
- 资产仅概括漫画风格、多格、气泡和效果表现，不再将具体样例动作当作固定人物属性；这属于候选能力描述，不保证权重独自实现所有效果。
- 任务 `0c520c5e5f` 的引用存在且警告为空，但 IR 出现 `no multiple panels` / `no text or speech bubbles`，最终正负文本均未执行作者模板。这证明引用合法与模型 `usage_applied` 不能作为模板执行证据。
- 根因之一是 Composer 规则把“一次输出”错误解释为“禁止多格”；同时系统只有不可信原文参考，没有用户选择模板和代码级执行。没有完整模型推理记录，不能把错误规则断言为唯一原因。
- 当前新增独立模板选择：漫画基础、单男场景、群体场景。选择物理 LoRA 默认不套模板；显式选择后，正向骨架、作者负面追加和主体计数由代码保证，Composer 只补当前人物/服装/场景。明确单格意图只停用多格布局部分。
- 生产 SQLite 当前不可变记录为 `6fab6a64ed09177b6a9018f8992bc9f9f88948da7c7183d94efa696b7cfa8931`，旧记录仍保留。新记录继续 `source_kind=author / verified=unverified`，不冒充外部来源或画质验证。832×1216 只是 UI 建议；作者花括号姿势候选不会被普通 CLIP 节点展开，现作为可见选项交给中文构思选择，不把花括号原样注入。

真实 Registry 为用户维护的 skip-worktree 文件。本轮经用户授权只将 FComic 的 `usage.refs` 切换到新记录；不暂存、不推送、不重建其他条目。生产资料写入前已创建并校验 `server/state/pre-fcomic-template-20260913-203125.zip`。密钥不得进入日志、文档或提交。

## 第一步：重启后复测模板执行

用户重启当前服务、刷新页面后选择 FComic；叠加栈应先显示“不套模板”，再由用户点选一个模板。先查看模板详情与最终正负文本，再决定是否生图。另保留一个无 usage 的普通 LoRA 对照，关注流程兼容而非开展批量画质评分。

收集最小证据：用户中文意图、已选 LoRA、最终正负文本、资料说明与警告、任务 ID，以及用户对结果的判断。尺寸建议当前不会自动覆盖用户设置，测试需用户自行选定。不要索要 config 或 token，不因失败自动重画。

通过条件：三个模板均可独立选择；漫画骨架和作者负面出现在最终执行值；单男/群体模板主体计数正确；“画一张”不关闭多格，明确“单格”才关闭；确认后保存/执行文本与预览一致；用户删掉的 final 不补回；普通 LoRA 与 manual 不回归。画面是否符合漫画目的由用户判断。

## 第二步：只修复已复现的问题

### A. 已完成：加载语法被误提取成 required_tags

定位 `.tools/register_lora.py` 的 `normalize_agent_candidate` 与候选生成提示。增加代码级校验，识别完整或去掉尖括号的 LoRA 加载表达式，拒绝它进入 exact trigger 列表并给出可理解的修订理由；不要只改提示词，也不要自动把所有类似文本删除后宣称提取成功。真正触发词和加载节点是两回事；缺少触发词证据只能标记未知，不能宣称作者明确说明无需触发词。

最低回归：伪加载标签拒绝、真实触发词保留、required 策略空列表拒绝、正常 character/style 候选不变。不得修改用户已经修正好的 FComic 条目。范围限候选提取/校验与相关测试，修复前重读实际调用方。

完成结论（2026-09-13）：候选提示已明确区分加载语法与 trigger；代码同时识别 `<lora:名称:强度>` 及去掉尖括号的形式，发现后拒绝整个候选并给出修订理由，不静默删除。新增回归覆盖 asset/Profile 伪加载表达式、真实 trigger 保留；既有 required 空列表和正常 character/style 路径继续通过。相关 onboarding agent 21 项、onboarding usage 18 项、usage 23 项与 Python 编译检查通过；未修改 FComic Registry 条目、生产数据库，也未调用模型/GPU。

### B. 已完成：补齐特殊 LoRA 模板执行

已按资料未提供、模型采用声明、结构遗漏、负面、最终化/提交五层核查。资料本身完整，但模型输出遗漏模板；后端现将显式模板当作用户锁而非可采纳参考，预览和入队都按当前不可变记录重新校验，任务保存模板快照与来源。

没有把作者模板塞进 `required_tags` 或 `provides`，也没有改权重、workflow 或尺寸。结构只覆盖本次反复出现的模板选择/继承/正负骨架/多格布局，不扩成通用规则语言。自动验收已覆盖原失败形态和无模板普通路径；未调用真实模型/GPU，不能据此宣称画质通过。

### C. 已知工程边界，不默认扩建

资料库与 Registry 文件之间没有跨资源原子事务：写资料后写文件失败会留下可复用记录，不能声称全部回滚。两次并发检查降低覆盖风险，但不是跨进程锁；若确实出现同时注册丢更新，再评估锁或比较后替换。旧版本原文必须可追溯。

## 第三步：验收后继续暂停

修复按范围运行 `.tools/test_lora_onboard_agent.py`、`.tools/test_lora_onboarding_usage.py`、`.tools/test_lora_usage.py`；改动消费链时再跑 P2B/最终文本相关测试。所有测试隔离 state/Registry，mock 不能冒充真实画质。一次通过后不重复无关全集。

通过后只同步受影响现状文档、显式提交并 push。普通修订并入阶段结果，不新开 ADR 或流水日志。无复现问题时停止，不开启通用 Agent、自动工作流、参数优化或前端重构。若连续两次定向修复仍未解释同一问题，整理失败证据、已尝试方案和最小复现，再请原主 Agent 协助；不要继续盲试。

## 文件清理记录

用户要求清理后，已移除此前 `.local-backups/handoffs-20260913/` 中五份过期搬运副本：`comfy-web - 开发1.md`、`LORA_USAGE_EXECUTION_PLAN.md`、`LORA_USAGE_CONTRACT_REVIEW.md`、`LORA_USAGE_P1_TASK.md`、`LORA_USAGE_P2_TASK.md`。它们不再是可打开的接手依赖，历史引用仅保留出处含义。

保留契约 §8–10、资料示例、当前测试与操作脚本：这些仍用于维护和回归，并非一次性产物。保留 config 恢复备份、封版安全副本、stash、用户模型/图片/数据库；本轮不清理这些数据。
