# P2 后的有限维护与实测计划

更新：2026-09-13。交接基线：`3749b57`（P2 工程基线为 `c175e11`）。负责人转交 5.6sol；此前主 Agent 仅在多次定向修复仍不能解决的困难问题上参与。本文是用户明确要求的一份交接计划，不自动授权开启新产品阶段。

## 当前结论与边界

P2 工程已交付；用户自行进行浏览器测试。先读 `E:/comfy-web/AGENTS.md`、`E:/comfy-web/docs/BUILDHANDOFF.md`，具体协议查 `E:/comfy-web/docs/LORA_USAGE_CONTRACT.md` §8–10。不要通读已废弃的搬运任务书，勿按任务或测试轮次追加 DEVLOG/ADR。

2026-09-13 只读检查用户刚登记的 `fcomic1to1000_anima_v1`：

- Registry 文件为 `E:/comfy-web/server/lora_registry.yaml`，资产类型 style、trigger_policy=none、required_tags=[]、model/clip 强度均为 1.0；本地模型文件存在。先前误把加载标签当 trigger 的候选已被用户修订，没有进入当前条目。
- 资产仅概括漫画风格、多格、气泡和效果表现，不再将具体样例动作当作固定人物属性；这属于候选能力描述，不保证权重独自实现所有效果。
- usage 引用 `d5d506f35bc97e9bc7159280a2c7e8848d7cd8f953cb48446aaf53950254714b` 在生产 SQLite 中存在，asset_key 匹配、profile_id 为空（资产共享）；正文与完整版本 ID 校验通过。
- 正文 2249 字符，包含负面段落、漫画基础模板、人物占位符和 832×1216 建议；source_kind=author 是用户录入的来源标签，verified=unverified。candidate/advisory/background 为空不妨碍 P2B 读取原文。本次没有对完整原文做外部来源核验，也没有调用模型或 GPU。
- 结论仅为登记/关联正确，可以开始浏览器试用。未证明模型会采用资料、不会遗漏模板、不会过度套用示例，更未证明画质达标。不要自动更改 verified。

真实 Registry 为用户维护的 skip-worktree 文件，不暂存、不同步、不重建；本轮未修改它或生产数据库。旧 ningen_mame 已从本地 config 兼容列表移除，新 ningenmame_v2 风格资产保留；config 是启动时读取，需重启服务才生效。密钥不得进入日志、文档或提交。

## 第一步：等待用户浏览器结果，不先改代码

用户重启当前服务、刷新页面后选择新 LoRA，通过中文构思先预览，再决定是否生图。建议用非露骨的简单漫画场景验证多格结构；再用普通单幅意图检验资料是否被无条件套用。另保留一个无 usage 的普通 LoRA 对照，关注流程兼容而非开展批量画质评分。

收集最小证据：用户中文意图、已选 LoRA、最终正负文本、资料说明与警告、任务 ID，以及用户对结果的判断。尺寸建议当前不会自动覆盖用户设置，测试需用户自行选定。不要索要 config 或 token，不因失败自动重画。

通过条件：页面能选到资产；预览能解释本次是否采用资料；模型需要的模板信息出现在最终正向中；冲突负面按意图调整；确认后保存/执行文本与预览一致；普通 LoRA 与 manual 行为不回归。画面是否符合漫画目的由用户判断。

## 第二步：只修复已复现的问题

### A. 已完成：加载语法被误提取成 required_tags

定位 `.tools/register_lora.py` 的 `normalize_agent_candidate` 与候选生成提示。增加代码级校验，识别完整或去掉尖括号的 LoRA 加载表达式，拒绝它进入 exact trigger 列表并给出可理解的修订理由；不要只改提示词，也不要自动把所有类似文本删除后宣称提取成功。真正触发词和加载节点是两回事；缺少触发词证据只能标记未知，不能宣称作者明确说明无需触发词。

最低回归：伪加载标签拒绝、真实触发词保留、required 策略空列表拒绝、正常 character/style 候选不变。不得修改用户已经修正好的 FComic 条目。范围限候选提取/校验与相关测试，修复前重读实际调用方。

完成结论（2026-09-13）：候选提示已明确区分加载语法与 trigger；代码同时识别 `<lora:名称:强度>` 及去掉尖括号的形式，发现后拒绝整个候选并给出修订理由，不静默删除。新增回归覆盖 asset/Profile 伪加载表达式、真实 trigger 保留；既有 required 空列表和正常 character/style 路径继续通过。相关 onboarding agent 21 项、onboarding usage 18 项、usage 23 项与 Python 编译检查通过；未修改 FComic Registry 条目、生产数据库，也未调用模型/GPU。

### B. 仅当真实试用失败：模板使用不当

先分辨资料未提供、模型未声明采用、采用但遗漏结构、负面冲突、最终化/提交丢值，还是生图模型能力限制。定位 `server/prompt_engine.py` 的资料收集、USAGE 解析/校验与 Composer 请求，`server/api.py` 的预览/任务/分支传递。先核对实际上下文与已保存文本，再决定修哪里。

不要把完整作者模板塞进 required_tags，也不要把模板要求伪装成权重“已经提供”的属性。原文已包含所需信息时不强迫新建结构化字段。作者不同模型版本的总说明不自动导入；只用用户明确提供的当前版本文本。修复后使用一个复现用例与一个普通路径对照，不用随机反复生图掩盖问题。

### C. 已知工程边界，不默认扩建

资料库与 Registry 文件之间没有跨资源原子事务：写资料后写文件失败会留下可复用记录，不能声称全部回滚。两次并发检查降低覆盖风险，但不是跨进程锁；若确实出现同时注册丢更新，再评估锁或比较后替换。旧版本原文必须可追溯。

## 第三步：验收后继续暂停

修复按范围运行 `.tools/test_lora_onboard_agent.py`、`.tools/test_lora_onboarding_usage.py`、`.tools/test_lora_usage.py`；改动消费链时再跑 P2B/最终文本相关测试。所有测试隔离 state/Registry，mock 不能冒充真实画质。一次通过后不重复无关全集。

通过后只同步受影响现状文档、显式提交并 push。普通修订并入阶段结果，不新开 ADR 或流水日志。无复现问题时停止，不开启通用 Agent、自动工作流、参数优化或前端重构。若连续两次定向修复仍未解释同一问题，整理失败证据、已尝试方案和最小复现，再请原主 Agent 协助；不要继续盲试。

## 文件清理记录

用户要求清理后，已移除此前 `.local-backups/handoffs-20260913/` 中五份过期搬运副本：`comfy-web - 开发1.md`、`LORA_USAGE_EXECUTION_PLAN.md`、`LORA_USAGE_CONTRACT_REVIEW.md`、`LORA_USAGE_P1_TASK.md`、`LORA_USAGE_P2_TASK.md`。它们不再是可打开的接手依赖，历史引用仅保留出处含义。

保留契约 §8–10、资料示例、当前测试与操作脚本：这些仍用于维护和回归，并非一次性产物。保留 config 恢复备份、封版安全副本、stash、用户模型/图片/数据库；本轮不清理这些数据。
