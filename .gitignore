# 含密钥/邀请码, 永不提交
config.yaml

# 运行时产物
images/
__pycache__/
*.pyc
*.log
server/lora_cache.json
server/knowledge_cache/
server/state/
server/backups/
# 旧批量评测资产已清理，不再入仓库（结论见 docs/decisions.md 与 DEVLOG）
.tools/eval_set/

# 便携版 gh CLI (大文件, 自行下载即可, 非源码)
.tools/gh/

# Agent 工具数据目录 (memory/skills/... 不进项目代码仓库)
.workbuddy/

# 本地迁移/工具备份, 不属于项目源码
.local-backups/
.tools/.artifacts/
