# 含密钥/邀请码, 永不提交
config.yaml

# 运行时产物
images/
__pycache__/
*.pyc
*.log
server/lora_cache.json
.tools/eval_set/candidate*.yaml
.tools/eval_set/render_exp/output/

# 便携版 gh CLI (大文件, 自行下载即可, 非源码)
.tools/gh/

# web/ 是独立 git 仓库 (air041001/air), 不纳入本仓库避免子模块混乱
web/

# Agent 工具数据目录 (memory/skills/... 不进项目代码仓库)
.workbuddy/
