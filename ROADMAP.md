# AirPaint Roadmap

> 当前版本进入封版维护。本文件只保留仍有效的维护边界；完成证据见 `docs/BUILDHANDOFF.md`，历史取舍见 `docs/decisions.md` 与 `docs/DEVLOG.md`。

## 当前状态

Prompt Composer、角色知识、LoRA Binding、单一 Anima workflow、参考契约、整图 Img2Img、历史分支和 SQLite 恢复链已经组成可长期保存的最终版本。

D59 的工程链路已经验证，但参考图/Img2Img 五张定向样本的人眼结果没有全部通过。封版保留功能并准确说明限制，不继续用增加 Prompt、提高 denoise 或重复抽图解决图像编辑质量。

## 封版后的维护范围

仅处理以下会阻止项目继续使用的问题：

- 任务、会话、用量或图片引用丢失、重复扣次、重复提交。
- 重启恢复、结果取回、备份恢复或归属检查失效。
- 当前依赖、ComfyUI 或浏览器更新造成的明确兼容故障。
- 可复现的鉴权、越权读取或敏感信息泄露。
- 阻断桌面/移动端现有主流程的界面错误。

普通修复仍需最小相关测试、文档同步、显式暂存和 push；不重新开启图像质量跑分。

## 明确停止

- 不主动新增模型、Prompt 优化方案、RAG、微调、多 Agent、自动改图或新 workflow。
- 不恢复 ControlNet、IP-Adapter、区域提示词、局部重绘或 inpaint。
- 不建设大规模 LoRA 推荐、marketplace、用户画像、社交或向量数据库。
- 不迁移前端框架，不改造成微服务、分布式队列或多 GPU 平台。
- 不用更长 Prompt、更多 IR 字段、长负面词或批量 A/B 代替人眼质量判断。

发现新的创作摩擦时先在 ComfyUI 中确认它是否可复现、是否有小而明确的修复；只有阻止现有能力使用时才进入维护实施。
